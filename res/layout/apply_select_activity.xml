<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/common_gray_bg"
    android:orientation="vertical" >

    <include layout="@layout/wx_common_title" />

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="400dp"
        android:background="@drawable/img_prediction_register"
        android:gravity="center"
        android:orientation="horizontal" >

        <ImageView
            android:id="@+id/apply_select_primary"
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:layout_marginLeft="-10dp"
            android:layout_marginTop="20dp"
            android:src="@drawable/img_prediction_primary" />

        <ImageView
            android:id="@+id/apply_select_junior"
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:layout_marginLeft="50dp"
            android:layout_marginTop="20dp"
            android:src="@drawable/img_prediction_midden" />
    </LinearLayout>

</LinearLayout>